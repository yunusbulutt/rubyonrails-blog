<%= form_for([@post, @post.comments.build]) do |f| %>
<%= f.label :İsminiz %><br>
<%= f.text_field :name %><br>
<br>
<%= f.label :Yorumunuz %><br>
<%= f.text_area :body %><br>
<br>
<%= f.submit :Gönder %>
<% end %>