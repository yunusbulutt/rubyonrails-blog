<% @posts.each do |post| %>
<div class="post_wrapper">
    <h2 class="title"><%= link_to post.title, post %></h2>
    <p class="date"><%=l post.created_at, format: :long , :locale => 'tr' %>, tarihinde yazıldı.</p>
</div>
<% end %>